/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "0lhkvrapezrozbvv.public.blob.vercel-storage.com",
      "cdn.sanity.io",
      "via.placeholder.com",
    ],
  },
};

export default nextConfig;
